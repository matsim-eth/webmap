services:
  webmap_backend:
    image: webmap-backend:dev
    build:
      context: ./webmap-backend
      target: dev
    volumes:
      - ./webmap-backend:/app
      - dataset_storage:/data/datasets:ro
    environment:
      ENV: dev
