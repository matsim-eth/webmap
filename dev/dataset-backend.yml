services:
  dataset_backend:
    image: dataset-backend:dev
    build:
      context: ./dataset-backend
      target: dev
    volumes:
      - ./dataset-backend:/app
      - dataset_storage:/data/datasets
    environment:
      UVICORN_WORKERS: "1"
