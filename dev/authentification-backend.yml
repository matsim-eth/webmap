# NOTE: dev/all.yml contains a copy of this service block — keep both in sync.
services:
  authentification_backend:
    image: authentification-backend:dev
    build:
      context: ./authentification-backend
      target: dev
    volumes:
      - ./authentification-backend:/app
    environment:
      UVICORN_WORKERS: "1"
      ENV: dev